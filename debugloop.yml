---
- name: Print Messages
  hosts: all
  become: true
  gather_facts: false

  tasks:

    - name: first group 1 2 3
      debug:
        msg: "Host: {{ item }}"
      loop: "{{ ansible_play_hosts }}"
      delegate_to: "{{ item }}"
      run_once: true

    - name: second group 7 8 9
      debug:
        msg: "Host: {{ item }}"
      loop: "{{ ansible_play_hosts }}"
      delegate_to: "{{ item }}"
      run_once: true

    - name: third group 4 5 6
      debug:
        msg: "Host: {{ item }}"
      loop: "{{ ansible_play_hosts }}"
      delegate_to: "{{ item }}"
      run_once: true

    - name: fourth group 10 11 12
      debug:
        msg: "Host: {{ item }}"
      loop: "{{ ansible_play_hosts }}"
      delegate_to: "{{ item }}"
      run_once: true
