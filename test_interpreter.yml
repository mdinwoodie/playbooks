---
- hosts: localhost
  gather_facts: false
  #connection: local
  tasks:
    - name: try python
      shell: echo 'hello world'
      
    - debug:
        msg: "ansible_playbook_python: {{ ansible_playbook_python }}"
