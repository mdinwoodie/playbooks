---
- name: Listen for events on a webhook from webhook
  hosts: localhost
  sources:
    - ansible.eda.webhook:
        host: 0.0.0.0
        port: 5002
  rules:
    - name: Read raw payload 
      condition: event.meta is defined or event.payload.message == "test"
        #any:
          #- event.meta is defined
          #- event.payload is defined
          #- event.payload.message == "Ansible is super cool!"
      action: 
        debug:
          var: event.payload.message


# Trigger rulebook with the following command:
# curl --header "Content-Type: application/json" --request POST --data '{"message":"test"}' http://$EDA_CONTROLLER:5002
